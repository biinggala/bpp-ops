# 변경 이력

## v1.2.1

데스크톱 앱의 구글 연동이 한 시간마다 풀리던 문제를 고쳤습니다.

**받는 곳**
- 웹 — https://crng-task-manager.web.app (별도 설치 없이 바로)
- macOS 앱 — 이 릴리즈 하단의 `.dmg`
- Claude 커넥터 — `https://crng-task-manager-1050546278891.asia-northeast3.run.app/mcp`

### 고친 것

**데스크톱 앱에서 캘린더 연동이 갑자기 풀리고, 다시 연동해도 안 되던 문제**

세 겹이었습니다.

1. **한 시간마다 풀렸습니다.** 액세스 토큰은 한 시간짜리고, 브라우저에서는 구글 세션이 살아 있는 동안 조용히 갱신됩니다. 데스크톱에는 그 방법이 없어서 만료되면 그대로 끊겼습니다. 이제 앱이 **갱신용 토큰(refresh token)을 직접 보관**합니다 — 이건 클라이언트 시크릿을 가진 쪽만 쓸 수 있는 방식이라 웹에서는 불가능하고 앱에서는 가능합니다. 한 번 연동하면 창이 뜨는 일 없이 계속 유지됩니다. 토큰은 웹뷰가 아니라 **앱 데이터 폴더에 600 권한으로** 저장돼서, 화면 쪽 코드는 만질 수 없습니다.

2. **'다시 연동'이 진짜 이유를 감췄습니다.** 실패하면 예전 팝업 방식으로 넘어가게 돼 있었는데, 그 팝업이 바로 구글이 앱 내장 웹뷰에서 거부하는 화면입니다. 그래서 원래 오류가 "팝업이 안 열린다" 같은 엉뚱한 말로 덮였습니다. 앱에서는 이제 넘어가지 않고 실제 이유를 그대로 보여줍니다.

3. **재연동 버튼이 실패해도 아무 말을 안 했습니다.** 눌러도 버튼만 그대로 돌아와서 클릭이 씹힌 것처럼 보였습니다. 이제 버튼 옆에 실패 사유가 표시됩니다.

'연동 해제'를 누르면 보관한 갱신 토큰도 함께 지워집니다. 드라이브도 같은 경로를 쓰므로 함께 적용됩니다.

**리스트 뷰에서 열 설정 체크박스를 끄면 메뉴가 닫히던 문제**
체크박스가 켜져 있을 때만 ✓ 표시를 그리고 있어서, 체크를 끄는 순간 클릭 지점에 있던 ✓ 노드가 사라졌습니다. 바깥 클릭 감지가 "이 클릭이 우리 메뉴 안에서 났나"를 물을 때 대상이 이미 없어서 바깥으로 판정했습니다. 담당자·태그·필터 등 다른 다중 선택 메뉴도 같은 상황이었습니다.

> 이 갱신 기능은 **v1.2.1 앱부터** 동작합니다. 상단 `업데이트` 버튼이나 아래 `.dmg`로 받아주세요.

## v1.2.0

데스크톱 앱 두 가지를 고쳤습니다.

**받는 곳**
- 웹 — https://crng-task-manager.web.app (별도 설치 없이 바로)
- macOS 앱 — 이 릴리즈 하단의 `.dmg`
- Claude 커넥터 — `https://crng-task-manager-1050546278891.asia-northeast3.run.app/mcp`

### 고친 것

**데스크톱 앱에서 구글 캘린더·드라이브 연동이 안 되던 문제**
연동 버튼이 아무 반응도 없는 것처럼 보였습니다. 구글은 앱 내장 웹뷰에서 자기 로그인 화면을 거부하는데, 연동 토큰을 받는 방식이 하필 그 화면을 팝업으로 띄우고 있었습니다. 로그인이 이미 쓰고 있던 방식 — 시스템 브라우저로 나갔다가 되돌아오는 설치형 앱 표준 절차 — 로 바꿨습니다. 드라이브도 같은 경로를 쓰므로 함께 고쳐집니다.

데스크톱에서는 조용한 자동 갱신이 없습니다(갱신하려면 브라우저 창이 떠야 하므로). 만료되면 연동 버튼이 다시 나타나고, 한 번 눌러주면 됩니다.

### 새로 생긴 것

**앱 안에서 업데이트 알림**
새 버전이 나오면 상단에 `업데이트 v1.2.0` 버튼이 뜹니다. 누르면 브라우저로 다운로드 페이지가 열립니다.

앱은 배포된 웹을 그대로 불러오기 때문에 **화면과 기능은 이미 항상 최신**입니다. 낡는 건 껍데기(바이너리)뿐이고, 그건 자주 바뀌지 않습니다. 그래서 이 버튼은 할 일이 있을 때만 나타나고 평소에는 보이지 않습니다 — 매일 "최신입니다"라고 말하는 버튼은 아무도 안 읽으니까요.

> **이번 한 번은 직접 받아주세요.** v1.1.0 이하 앱에는 이 확인 기능 자체가 없어서, v1.2.0을 설치한 다음부터 알림이 옵니다.

## v1.1.0

데이터가 프로젝트 단위로 나뉘고, 캘린더·리스트·간트가 새로 짜였고, 구글 드라이브가 붙었습니다. v1.0.0 이후 커밋 70여 개.

**받는 곳**
- 웹 — https://crng-task-manager.web.app (별도 설치 없이 바로)
- macOS 앱 — 이 릴리즈 하단의 `.dmg`
- Claude 커넥터 — `https://crng-task-manager-1050546278891.asia-northeast3.run.app/mcp`

### 구조

**데이터가 프로젝트 단위로 나뉘었습니다**
전에는 모든 업무가 한 뭉치에 있고 화면에서 걸러 보여줬습니다. 즉, 권한은 UI의 약속이었지 데이터의 성질이 아니었습니다. 이제 DB가 프로젝트별로 갈라져 있고 접근 규칙이 프로젝트 멤버십을 직접 검사합니다 — 멤버가 아니면 애초에 읽히지 않습니다. 기존 데이터는 변환 스크립트로 옮겼고, 새 규칙에 맞는지 검증한 뒤 전환했습니다.

### 새로 생긴 것

**구글 드라이브 연동**
업무에 드라이브 파일을 붙일 때 **URL을 붙여넣지 않습니다.** 검색해서 고르면 파일 ID로 저장되므로, 나중에 이름이 바뀌거나 폴더를 옮겨도 링크가 살아 있고 지금 이름으로 표시됩니다.

파일명뿐 아니라 **내용까지 검색**하고, 검색어가 들어간 문장을 미리보기로 보여줍니다. 구글 독스가 탭으로 나뉘어 있으면 **그 문구가 있는 탭으로 바로** 링크됩니다 — 회의록 하나에 출연자 다섯 명 탭이 들어 있어도 필요한 탭이 열립니다.

**자료 탭 (통계 다음)**
프로젝트마다 계약서·브랜드 가이드처럼 "업무가 아니라 선반에 있는" 파일을 두는 자리입니다. 아래쪽에는 업무에 붙은 자료가 마일스톤별로 색인돼 있어, 어느 업무에 뭐가 붙었는지 한눈에 보입니다. 링크마다 부제를 달 수 있어서 같은 이름의 미팅록도 구분됩니다.

폴더도 자료 중 하나입니다. 전에는 "프로젝트 드라이브 폴더"가 따로 있어서 같은 성격의 것을 두 군데에 두고 있었고, 폴더는 하나밖에 못 담았습니다. 이제 폴더는 그냥 자료 목록에 넣으면 되고, 그 폴더가 드라이브 검색 범위가 됩니다. 기존에 등록해둔 폴더는 자료 뷰를 열 때 자동으로 목록으로 옮겨집니다.

**캘린더 하나로**
월 뷰와 타임라인이 따로 있던 것을 **일 / 3일 / 주 / 월** 전환 하나로 합쳤습니다.
- 타임라인에서 드래그해 구글 캘린더 일정을 만들고, 옮기고, 늘리고, 지우고, 팀원을 초대합니다. 계정이 볼 수 있는 **모든 캘린더**를 읽습니다.
- 열면 오전 9시가 위에 옵니다 — 새벽 여섯 칸을 매번 스크롤로 지나칠 이유가 없습니다.
- **날짜를 클릭하면** 그 자리에서 업무를 만들거나, 일정 없는 업무를 끌어다 그 날에 놓습니다.
- **스크롤로 시간을 넘깁니다.** 애플 캘린더처럼 하루씩 따라 움직이고, 손을 떼면 날짜 경계에 스르륵 멈춥니다.

**간트 — 레인이 곧 입력창**
전에는 이미 날짜가 있는 업무만 그렸습니다. 나머지는 빈 줄만 남기고, 고치려면 모달로 나가야 했습니다.
- **빈 레인을 드래그하면** 그 기간이 그 업무의 일정이 됩니다.
- **마일스톤 레인을 드래그하면** 그 기간짜리 업무가 그 자리에서 생깁니다. 마일스톤 행마다 `+ 업무` 버튼도 있고, Enter로 계속 이어 붙습니다.
- 막대 양끝을 잡아 시작일·마감일을 따로 조정하고, 드래그 중에는 확정될 날짜가 보입니다.
- **일 / 주 / 월 줌**, `오늘` 버튼, **의존관계 화살표**(데이터엔 늘 있었는데 그림엔 없었습니다), 날짜 없는 업무 수를 세는 `미배치 N`.

**마감일 피커**
날짜를 고를 때 OS 기본 달력 대신, **그 날에 일이 얼마나 몰려 있는지**를 막대로 보여주는 달력이 뜹니다. 마일스톤·선행 업무의 마감일이 함께 표시되고, 날짜에 올리면 그 날 마감인 업무가 아래에 나열됩니다.

**리스트 뷰**
그룹 기준(프로젝트·마감·우선순위·담당자·상태·없음)과 컬럼 표시/숨김을 고를 수 있습니다. 제각각이던 드롭다운을 하나의 규격으로 통일했고, 업무를 추가하는 경로도 "그릇마다 하나"로 정리했습니다.

**마일스톤은 고르는 자리에서 만듭니다**
프로젝트로 묶지 않은 화면에는 마일스톤을 만들 자리가 아예 없었습니다. 이제 마일스톤 선택 메뉴 아래 `+ 새 마일스톤`이 있어서, 이름과 마감일만 넣으면 그 자리에서 만들어지고 바로 선택됩니다. 업무를 추가할 때도 담당자·마감일 옆에서 마일스톤을 함께 지정할 수 있고, 연달아 추가할 때는 직전 마일스톤이 유지됩니다.

**모바일**
필터·그룹·정렬을 한 시트에서 다루고, 터치마다 미세 진동이 옵니다. 마우스 기준으로 만들어진 팝오버들은 바텀시트로 다시 만들었습니다.

**MCP 도구 7개 → 20개**
마일스톤 만들기·수정·삭제, 팀원 목록, 현황 요약, 여러 업무 한 번에 수정, 업무·프로젝트 자료 링크 붙이기까지 대화로 됩니다.

**노션 팔레트**
앱 전체 색을 노션 라이트 팔레트로 맞췄습니다.

### 고친 것

**드라이브에 글자를 칠 때마다 구글 팝업이 뜨던 문제**
드라이브 API가 켜져 있지 않으면 403이 오는데, 이걸 토큰 만료로 잘못 읽고 있었습니다. 토큰을 버리고 → 다시 인증(팝업) → 또 403 → 반복. 이제 401만 만료로 취급하고, 403은 무엇을 켜야 하는지 한국어로 알려줍니다.

**담당자를 두 명 이상 지정하면 상세 화면에서 미배정으로 보이던 문제**
목록과 상세가 서로 다른 담당자 선택기를 쓰고 있었습니다. 게다가 상세 쪽을 한 번 건드리면 나머지 담당자가 조용히 지워지는 상태였습니다 — 표시만이 아니라 데이터 손실이었습니다.

**간트에서 막대가 업무 이름을 덮던 문제**
막대와 고정된 이름 칸이 같은 층에 있어서 나중에 그려지는 막대가 이겼습니다. 마일스톤 행에 마우스를 올리면 이름 칸 배경이 반투명해져 타임라인이 그대로 비쳐 보이기도 했습니다 — 고정 칸이 존재하는 이유가 바로 그걸 막는 것인데요.

**월 뷰 스크롤이 툭툭 끊기던 문제**
두 가지였습니다. 스크롤 이벤트마다 칸 크기를 실측하느라 강제 레이아웃이 끼어들었고, 각 날짜 칸이 자기 몫을 찾으려고 전체 업무 목록을 매번 훑고 있었습니다 — 56칸이면 56번입니다. 데이터는 한 번만 색인하고, 한 주를 넘길 때 실제로 바뀌는 7칸만 다시 그립니다.

**그 밖에**
- 드롭다운이 부모 요소에 잘려 화면 밖으로 나가던 문제 (포털로 분리)
- 완료된 업무가 마감이 가깝다는 이유로 맨 위에 오던 문제
- 프로젝트로 묶지 않으면 업무 추가 버튼이 사라지던 문제
- "내 할 일"에 들어갔는데 담당자 필터가 남아 있는 등 앞뒤가 안 맞던 필터 조합
- 사이드바 "내 할 일" 개수가 실제와 다르던 문제
- 마일스톤에서 날짜를 고른 뒤 Enter가 먹지 않던 문제
- 간트에서 한글 입력 시 업무가 하나 더 생기던 문제
- 캘린더 체크박스를 꺼도 화면이 그대로던 문제
- 캘린더 범위를 바꿀 때마다 구글에 다시 물어보던 문제

### 알아두면 좋은 것

- **Claude 커넥터를 한 번 껐다 켜 주세요.** 도구 목록을 다시 읽어야 20개가 됩니다.
- **드라이브는 처음 쓸 때 권한 승인이 필요합니다.** 문서 탭 링크까지 쓰려면 승인 화면에서 문서 읽기 권한도 함께 허용해야 합니다.
- 데스크톱 앱은 여전히 서명되지 않았습니다 — 아래 설치 안내를 참고하세요.

## v1.0.0

크린지 플로우에서 **bpp-ops**로 이름을 바꾸고, 웹 앱 하나였던 것을 데스크톱 앱과 MCP 서버까지 갖춘 형태로 확장한 릴리즈입니다.

**받는 곳**
- 웹 — https://crng-task-manager.web.app (별도 설치 없이 바로)
- macOS 앱 — 이 릴리즈 하단의 `.dmg`
- Claude 커넥터 — `https://crng-task-manager-1050546278891.asia-northeast3.run.app/mcp`

### 새로 생긴 것

**macOS 데스크톱 앱**
독에 올려두고 쓰는 네이티브 앱이 생겼습니다. 프론트엔드를 번들하지 않고 배포된 웹 앱을 직접 로드하므로, 웹을 배포하면 데스크톱 앱도 자동으로 최신이 됩니다 — 따로 업데이트할 필요가 없습니다. 용량은 약 5MB.

Google이 내장 웹뷰에서의 OAuth를 차단하기 때문에, 로그인은 시스템 브라우저를 거치는 설치형 앱 표준 방식(RFC 8252 · PKCE)으로 처리합니다.

**MCP 서버 — Claude에서 업무 조작**
claude.ai와 Claude Desktop에서 대화로 업무를 조회·생성·수정·삭제할 수 있습니다. "회의록 보고 태스크 8개 만들어줘" 같은 대량 작업에서 특히 값어치를 합니다.

각자 Google 계정으로 로그인하고, **자기가 멤버인 프로젝트만** 보입니다. 어느 프로젝트에도 속하지 않은 계정은 로그인 단계에서 거부됩니다.

**프로젝트 아카이브**
완료된 프로젝트를 우클릭 → 아카이브하면 사이드바 하단의 접힌 섹션으로 내려갑니다. 전체 업무·내 할 일·통계·필터에서 모두 빠지지만 **삭제가 아니라 숨김**이라, 클릭하면 내용은 그대로 다 남아 있습니다.

**완료 업무 숨기기**
상단의 토글 하나로 완료된 업무를 모든 뷰에서 숨깁니다. 하위 업무에도 적용됩니다.

**엑셀식 인라인 업무 추가**
마일스톤 아래 "새 업무 추가"를 누르면 모달 대신 빈 행이 생깁니다. 이름을 쓰고 Tab으로 담당자·마감일·상태를 채운 뒤 Enter로 저장, 이어서 다음 행이 열립니다. 마감일은 달력에서 고릅니다.

**통계 대시보드 개편**
요약 카드, 카테고리별 진행률, 마감 초과 목록, D-7 마감 예정, 담당자별 현황으로 재구성했습니다. 담당자 카드를 클릭하면 그 사람으로 필터된 리스트로 이동합니다.

### 고친 것

**업무가 저장되지 않던 문제**
인라인 행에서 Enter를 눌러도 업무가 사라지기만 하던 문제입니다. Firebase는 값이 `undefined`인 필드가 있으면 **동기적으로** 예외를 던지는데, 기존 `.catch()`는 비동기 거부만 잡고 있었습니다. 하위 업무가 아닌 태스크는 `parentId`가 `undefined`라 매번 여기 걸렸습니다.

**한글 입력 시 업무가 두 개 생기던 문제**
한글은 마지막 글자가 조합 중 상태로 남아 있어서, Enter 한 번이 `keydown`을 두 번 발생시킵니다. 첫 번째로 전체 텍스트가 저장되고 입력창이 비워진 뒤, IME가 마지막 글자를 다시 넣고 두 번째 Enter가 그걸로 또 저장하던 것입니다. Enter 핸들러 24곳 전체에 조합 상태 가드를 넣었습니다.

**통계 뷰에 권한 없는 사람이 보이던 문제 (보안)**
담당자별 현황과 담당자 필터가 "보이는 업무에 등장하는 모든 담당자"로 목록을 만들고 있었습니다. 그래서 프로젝트 멤버가 아닌 사람도 한 번이라도 담당자로 지정됐으면 전원에게 노출됐습니다. 이제 **해당 업무가 속한 프로젝트의 멤버**만 표시합니다.

**모바일 PWA 하단 여백**
iOS 설치형 PWA가 뷰포트 높이를 실제보다 작게 보고해서, 하단 네비게이션 바가 화면 바닥에서 한참 떠 있었습니다. 네비바를 고정 배치에서 일반 레이아웃 흐름으로 옮기고, 앱 높이를 실측값으로 잡도록 바꿨습니다.

**간트 차트 겹침**
스크롤할 때 왼쪽 고정 컬럼 뒤로 타임라인이 비쳐 보이던 문제입니다. 고정 컬럼의 배경이 반투명이었습니다. 모바일에서는 좌측 폭과 날짜 칸도 좁히고 막대 안 라벨을 숨겼습니다.

**앱 아이콘이 없던 문제**
`favicon.svg`가 저장소에 아예 없어서 PWA 아이콘이 계속 404였습니다. 회사 로고를 SVG로 넣고 PWA·macOS 아이콘을 여기서 생성합니다.

### 알아두면 좋은 것

- **데스크톱 앱은 서명되지 않았습니다.** 첫 실행 시 macOS가 막으므로, 앱을 `응용 프로그램`으로 옮긴 뒤 터미널에서 `xattr -cr "/Applications/bpp-ops.app"`를 한 번 실행해야 합니다.
- **번들 ID가 바뀌었습니다** (`kr.co.bpp.ops`). 이전 버전을 설치하셨다면 macOS가 별개 앱으로 인식하니 예전 것은 지우는 편이 깔끔합니다.
- **`@crngfriends.com` 주소는 그대로입니다.** 브랜드명이 아니라 기존 업무의 담당자 필드가 참조하는 실제 계정 식별자라, 바꾸면 과거 데이터의 담당자 매칭이 깨집니다.
