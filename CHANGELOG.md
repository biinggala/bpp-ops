# 변경 이력

## v1.0.0

크린지 플로우에서 **bpp-ops**로 이름을 바꾸고, 웹 앱 하나였던 것을 데스크톱 앱과 MCP 서버까지 갖춘 형태로 확장한 릴리즈입니다.

### 새로 생긴 것

**macOS 데스크톱 앱**
독에 올려두고 쓰는 네이티브 앱이 생겼습니다. 프론트엔드를 번들하지 않고 배포된 웹 앱을 직접 로드하므로, 웹을 배포하면 데스크톱 앱도 자동으로 최신이 됩니다 — 따로 업데이트할 필요가 없습니다. 용량은 약 5MB.

Google이 내장 웹뷰에서의 OAuth를 차단하기 때문에, 로그인은 시스템 브라우저를 거치는 설치형 앱 표준 방식(RFC 8252 · PKCE)으로 처리합니다.

**MCP 서버 — Claude에서 업무 조작**
claude.ai와 Claude Desktop에서 대화로 업무를 조회·생성·수정·삭제할 수 있습니다. "회의록 보고 태스크 8개 만들어줘" 같은 대량 작업에서 특히 값어치를 합니다.

각자 Google 계정으로 로그인하고, **자기가 멤버인 프로젝트만** 보입니다. 어느 프로젝트에도 속하지 않은 계정은 로그인 단계에서 거부됩니다.

**프로젝트 아카이브**
완료된 프로젝트를 우클릭 → 아카이브하면 사이드바 하단의 접힌 섹션으로 내려갑니다. 전체 업무·내 할 일·통계·필터에서 모두 빠지지만 **삭제가 아니라 숨김**이라, 클릭하면 내용은 그대로 다 남아 있습니다.

**완료 업무 숨기기**
상단의 토글 하나로 완료된 업무를 모든 뷰에서 숨깁니다. 하위 업무에도 적용됩니다.

**엑셀식 인라인 업무 추가**
마일스톤 아래 "새 업무 추가"를 누르면 모달 대신 빈 행이 생깁니다. 이름을 쓰고 Tab으로 담당자·마감일·상태를 채운 뒤 Enter로 저장, 이어서 다음 행이 열립니다. 마감일은 달력에서 고릅니다.

**통계 대시보드 개편**
요약 카드, 카테고리별 진행률, 마감 초과 목록, D-7 마감 예정, 담당자별 현황으로 재구성했습니다. 담당자 카드를 클릭하면 그 사람으로 필터된 리스트로 이동합니다.

### 고친 것

**업무가 저장되지 않던 문제**
인라인 행에서 Enter를 눌러도 업무가 사라지기만 하던 문제입니다. Firebase는 값이 `undefined`인 필드가 있으면 **동기적으로** 예외를 던지는데, 기존 `.catch()`는 비동기 거부만 잡고 있었습니다. 하위 업무가 아닌 태스크는 `parentId`가 `undefined`라 매번 여기 걸렸습니다.

**한글 입력 시 업무가 두 개 생기던 문제**
한글은 마지막 글자가 조합 중 상태로 남아 있어서, Enter 한 번이 `keydown`을 두 번 발생시킵니다. 첫 번째로 전체 텍스트가 저장되고 입력창이 비워진 뒤, IME가 마지막 글자를 다시 넣고 두 번째 Enter가 그걸로 또 저장하던 것입니다. Enter 핸들러 24곳 전체에 조합 상태 가드를 넣었습니다.

**통계 뷰에 권한 없는 사람이 보이던 문제 (보안)**
담당자별 현황과 담당자 필터가 "보이는 업무에 등장하는 모든 담당자"로 목록을 만들고 있었습니다. 그래서 프로젝트 멤버가 아닌 사람도 한 번이라도 담당자로 지정됐으면 전원에게 노출됐습니다. 이제 **해당 업무가 속한 프로젝트의 멤버**만 표시합니다.

**모바일 PWA 하단 여백**
iOS 설치형 PWA가 뷰포트 높이를 실제보다 작게 보고해서, 하단 네비게이션 바가 화면 바닥에서 한참 떠 있었습니다. 네비바를 고정 배치에서 일반 레이아웃 흐름으로 옮기고, 앱 높이를 실측값으로 잡도록 바꿨습니다.

**간트 차트 겹침**
스크롤할 때 왼쪽 고정 컬럼 뒤로 타임라인이 비쳐 보이던 문제입니다. 고정 컬럼의 배경이 반투명이었습니다. 모바일에서는 좌측 폭과 날짜 칸도 좁히고 막대 안 라벨을 숨겼습니다.

**앱 아이콘이 없던 문제**
`favicon.svg`가 저장소에 아예 없어서 PWA 아이콘이 계속 404였습니다. 회사 로고를 SVG로 넣고 PWA·macOS 아이콘을 여기서 생성합니다.

### 알아두면 좋은 것

- **데스크톱 앱은 서명되지 않았습니다.** 첫 실행 시 macOS가 막으므로, 앱을 `응용 프로그램`으로 옮긴 뒤 터미널에서 `xattr -cr "/Applications/bpp-ops.app"`를 한 번 실행해야 합니다.
- **번들 ID가 바뀌었습니다** (`kr.co.bpp.ops`). 이전 버전을 설치하셨다면 macOS가 별개 앱으로 인식하니 예전 것은 지우는 편이 깔끔합니다.
- **`@crngfriends.com` 주소는 그대로입니다.** 브랜드명이 아니라 기존 업무의 담당자 필드가 참조하는 실제 계정 식별자라, 바꾸면 과거 데이터의 담당자 매칭이 깨집니다.
