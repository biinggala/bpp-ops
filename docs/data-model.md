# 데이터 구조와 접근 권한

이사(마이그레이션) 후의 데이터 구조와, 누가 무엇을 열어볼 수 있는지 정리한 문서입니다.

## 왜 바꾸나

지금은 모든 업무가 `cringe/tasks` **한 곳에 배열로** 들어 있고, DB 규칙은 `cringe` 전체에 대해
`auth != null` 한 줄입니다. 결과는 셋입니다.

1. **권한이 없다.** 구글 계정으로 로그인한 사람은 누구나 전체 워크스페이스를 읽고 쓸 수 있습니다.
   `canAccessProject()`는 화면에서 숨길 뿐이고, DB에서 막지 않습니다.
2. **동시 편집이 유실된다.** 저장할 때마다 배열 전체를 덮어쓰므로, 두 사람이 비슷한 시각에
   저장하면 한 쪽 변경이 통째로 사라집니다.
3. **모든 클라이언트가 전부 내려받는다.** 앱이 `cringe` 루트를 구독하기 때문입니다.

한 곳에 뭉쳐 있는 한 규칙으로 막을 방법이 없습니다. 경로를 나누는 것이 전제입니다.

## 새 구조

```
teams/$tid/                        팀 (자리만 만들어 둠 — 지금은 사용하지 않음)
  meta/                            이름 등
  members/$uid: true

projects/$pid/
  meta/                            이름, 색, 마감일, 고객사, 초대코드, teamId, archived
  members/$uid: <초대코드>          이 프로젝트를 열 수 있는 사람
  tasks/$tid                       업무 (건당 한 칸)
  milestones/$mid                  마일스톤

userIndex/$uid/projects/$pid: true 내가 속한 프로젝트 목록
invitesByEmail/$이메일키/$pid       아직 로그인하지 않은 사람 앞으로 온 초대장
personalTasks/$uid/$tid            프로젝트 없는 개인 업무 (생성자 전용)

spaces/$sid                        스페이스(분류 라벨) — 워크스페이스 공통
userProfiles/$uid                  이름·이메일·사진
presence/$uid                      접속 상태
mcpAuth/                           MCP 서버 전용 (아무도 못 읽음)
```

프로젝트는 `teams/` 아래에 넣지 않고 **평면으로 둡니다.** 팀 간 이동이 생겨도 경로가 바뀌지
않아야 하기 때문입니다. 팀 소속은 `meta/teamId` 필드로만 표현합니다.

`$이메일키`는 이메일의 `.`을 `,`로 바꾼 값입니다 (`a.b@c.co.kr` → `a,b@c,co,kr`).
Firebase 키에 `.`을 쓸 수 없어서입니다.

## 누가 무엇을 열 수 있나

| 대상 | 읽기 | 쓰기 |
|---|---|---|
| `projects/$pid` 전체 | 그 프로젝트의 멤버만 | 멤버만 |
| `projects` (목록 전체) | **아무도 못 함** | — |
| `userIndex/$uid` | 본인만 | 본인만 |
| `invitesByEmail/$이메일키` | 이메일이 일치하는 본인만 | 초대하는 멤버, 또는 본인 |
| `personalTasks/$uid` | 본인만 | 본인만 |
| `userProfiles/$uid` | 로그인한 사람 (uid를 알아야 함) | 본인만 |
| `userProfiles` (목록 전체) | **아무도 못 함** | — |
| `spaces` | 로그인한 사람 전원 | 로그인한 사람 전원 |
| `mcpAuth` | 아무도 못 함 | 아무도 못 함 |

### 프로젝트 목록을 통째로 못 읽는 이유

Firebase는 상위 경로를 읽으려면 그 아래 **모든** 항목에 대한 권한을 요구합니다. 덕분에
`projects`를 통째로 훑는 것이 자동으로 막히지만, 대신 앱이 "내 프로젝트가 무엇인지" 알 방법이
필요합니다. 그것이 `userIndex`입니다.

앱은 `userIndex/$uid/projects`를 먼저 읽고, 거기 적힌 프로젝트에만 개별로 접속합니다.
구독이 1개에서 N+1개로 늘어나지만, 남의 데이터를 아예 받지 않게 됩니다.

### 초대가 동작하는 방식

초대 링크(`?invite=<코드>`)의 의미를 그대로 규칙으로 옮겼습니다.

- 프로젝트에는 `meta/inviteCode`가 있습니다.
- 로그인한 사람은 **자기 자신을** `members/$uid`에 넣을 수 있는데, 그 값이 프로젝트의
  `inviteCode`와 일치해야 합니다. 코드를 모르면 못 들어옵니다.
- 남의 uid를 대신 끼워넣는 것은 불가능합니다.
- 이메일로 초대한 경우, 상대가 처음 로그인했을 때 자기 앞으로 온 초대장을
  `invitesByEmail`에서 찾아 코드를 얻습니다.

Cloud Function 같은 서버 코드 없이 규칙만으로 처리됩니다.

## 아직 남는 노출

- **`spaces`는 로그인한 사람 전원이 읽고 씁니다.** 이름과 색뿐인 분류 라벨이라 우선순위를
  낮췄습니다. 다만 스페이스 이름에 고객사명을 쓰고 있다면 그건 노출됩니다. 팀 단위로
  나눌지는 Team 계층을 실제로 도입할 때 함께 정하는 편이 낫습니다.
- **`userProfiles/$uid`는 uid만 알면 읽힙니다.** 목록 열람은 막혀 있어 계정을 훑을 수는
  없지만, 같은 프로젝트 멤버가 아니어도 uid를 알면 이름과 이메일을 볼 수 있습니다.

## 검증

`test/rules.next.test.js`가 위 표의 내용을 에뮬레이터에서 검증합니다.
`test/rules.test.js`는 **현재** 규칙의 실태를 기록해 둔 것으로, 두 파일을 나란히 두면
이사로 무엇이 달라지는지가 그대로 드러납니다.

```
npm test
```

## 적용 시점

새 규칙은 `database.rules.next.json`에 따로 두었습니다. `deploy.yml`이 배포 때마다
`database.rules.json`을 실제 DB에 적용하기 때문에, 데이터를 옮기기 전에 규칙만 먼저
바뀌면 앱이 즉시 멈춥니다. 두 파일의 교체는 **이사 당일 마지막 단계**입니다.

## 태스크 자료 (Drive)

`TaskLink`에 `driveId`와 `mimeType`이 추가되었다. 기존 링크는 그대로 동작하며,
URL 안에 Drive 파일 ID가 들어 있으면 앱이 읽는 시점에 알아서 인식한다 —
마이그레이션은 필요 없다.

저장하는 것은 **ID뿐**이고, 이름·아이콘·수정 시각은 그릴 때마다 Drive에서 읽는다.
붙여넣은 URL은 붙여넣은 그 순간의 사진이라, 파일 이름이 바뀌면 태스크가 조용히
거짓말을 하기 시작한다. ID를 들고 있으면 그 문제가 생기지 않는다.

문서에 **탭이 여러 개**면 첨부는 그 키워드가 있는 탭을 가리킨다 (`tabTitle`에
탭 이름을 같이 저장). 탭은 Drive API로 보이지 않아서 **Google Docs API**
(`documents.readonly`)를 따로 쓰고, 그게 막혀 있으면 조용히 문서 전체 링크로
내려간다.

권한은 `drive.readonly`와 `documents.readonly`이며 GIS로 받는다. 앱은 Drive에 아무것도 쓰지 않고,
파일 내용도 가져오지 않는다. 메타데이터만 읽는다.

> **OAuth 동의 화면이 External이면** `drive.readonly`는 제한 범위라 "확인되지 않은 앱"
> 경고가 뜨고 사용자 100명 제한이 걸린다. Workspace 도메인이면 동의 화면을
> **Internal**로 두는 것이 맞고, 그러면 검수 없이 그대로 쓸 수 있다.

## 태스크 일정 (Google Calendar)

업무에 일정을 붙일 수 있다. **연결은 우리 DB가 아니라 구글 일정 쪽에 저장된다** —
일정의 `extendedProperties.private.bppTaskId`에 업무 ID를 쓴다. 업무에
`eventIds` 배열을 두지 않은 이유는 두 가지다.

- **안 썩는다.** 구글에서 일정을 옮기거나 이름을 바꾸거나 취소해도 연결은
  그대로 맞다. 우리 쪽에 낡아갈 사본이 없다.
- **접근 범위가 캘린더를 따라간다.** 업무 상세에 보이는 일정은 *그 사람이 볼 수
  있는* 캘린더의 일정뿐이다. 50명이 각자 개인 캘린더에 면접을 잡는데, 그걸 우리
  DB에 모아두면 모두의 일정표를 공유하는 것과 같아진다. 그건 원하는 바가 아니다.

찾는 방법은 `events.list?privateExtendedProperty=bppTaskId=<업무ID>`다. 업무 상세를
열 때 켜져 있는 캘린더마다 한 번씩 물어보고, 아무 곳에도 캐시하지 않는다 —
사람마다 답이 다르므로 캐시할 자리가 없다. 범위는 최근 1년.

두 가지 방식이 있고 성격이 다르다.

- **새 일정**: 제목·날짜·시간·초대할 사람을 받아 만든다. 구글이 초대 메일을
  보낸다(`sendUpdates=all`). 업무에서 만드는 이유가 바로 이것이다.
- **기존 일정 연결**: 이미 잡힌 일정을 붙인다. **아무에게도 아무것도 보내지
  않는다.** 초대는 그 일정을 만들 때 이미 끝났고, 업무 보드에서 다시 부르는 건
  소음이다.

연결 해제는 `bppTaskId`만 지운다. 일정은 건드리지 않는다 — 잘못 붙인 면접을
떼는 일이 면접을 취소하는 일이 되어서는 안 된다. `extendedProperties.private`는
PATCH 하면 맵 전체가 교체되므로, 지우기 전에 기존 값을 읽어 합친다.

권한은 일정 읽기(`calendar.readonly`)와 쓰기(`calendar.events`)를 연동할 때 함께
받는다. 연결을 저장하는 것도 일정에 쓰는 일이라 쓰기 권한이 필요하다.

## 활동 기록 (`projects/{pid}/activity/{taskId}`)

누가 무엇을 언제 바꿨는지. **프로젝트 노드 안에 둔다** — 그 노드는 이미 멤버만
읽고 쓸 수 있고 자식이 그것을 물려받으므로, 업무의 이력은 그 업무를 볼 수 있는
사람과 정확히 같은 사람에게 보인다. 새 규칙도, '누가 볼 수 있나'에 대한 새 개념도
필요 없다.

**한 번의 편집이 한 줄이다.** 상태와 마감일을 같은 동작으로 바꾼 것은 일어난 일
하나이고, 필드마다 줄을 나누면 평범한 오후가 마흔 줄이 되어 아무도 마흔 번째를
읽지 않는다.

**개인 업무는 기록하지 않는다.** 주인 말고는 고칠 수 있는 사람이 없어서, 남겨봐야
한 사람의 이름만 반복된다.

읽기는 열려 있는 동안에만, 마지막 50개만 구독한다. 업무 200개짜리 목록이 로그
200개를 함께 들고 다닐 이유가 없다.

## 알림 (`/notices`, `/pushSubs`)

알림은 **받는 사람의 자리에 직접 쓴다** — `/notices/{받는사람 uid}/{알림}`. 서버를
거치지 않으므로 수신함은 푸시 인프라와 무관하게 동작한다. 규칙은 읽기를 본인에게만
허용하고, 쓰기는 본인 또는 *아직 없는 노드에 대한 첫 쓰기*만 허용한다. 그래서 남에게
알림을 남길 수는 있어도, 남의 알림을 고치거나 지울 수는 없다.

무엇이 알림이 되는가는 한 가지 질문으로 정한다: **이 사람이 그러지 않으면 모르고
지나갈 일인가.** 담당자로 지정된 것, 내가 든 일의 마감일이 바뀐 것, **남이 내 일의
상태를 옮긴 것**, 내 업무 아래 하위 업무가 생긴 것 — 여기까지다. 태그나 메모 같은
편집은 보고 있는 화면의 몫이다.

상태 변경은 담당자에게만 간다. 그리고 상태를 옮기는 사람은 보통 자기 일을 하는
담당자 본인이고 본인에게는 알리지 않으므로, 실제로 울리는 경우는 *남이* 내 일을
옮긴 때 하나로 남는다 — 검토중으로 갔다면 내 차례라는 뜻이고, 완료로 갔다면 더는
내 일이 아니라는 뜻이다.
50명이 하루 종일 편집하는 도구에서 이 선을 풀면 수신함은 일주일이면 쓸모없어진다.
본인이 한 일은 본인에게 알리지 않는다.

`/pushSubs/{uid}/{기기 id}`는 Web Push 구독(endpoint + 키)이다. 본인만 읽고 쓴다.
기기 id는 `localStorage`에 두므로 같은 기기에서 다시 켜면 쌓이지 않고 교체된다.
발송은 Cloud Run(`mcp/src/push.ts`)이 하고, 죽은 구독(404/410)은 그 자리에서 지운다.

> **규칙 파일에 주석을 달지 마세요.** `"//"` 같은 키는 JSON 으로는 유효하지만
> Firebase 규칙에서는 키에 `/` 를 쓸 수 없어 **파일 전체가 거부됩니다.** 그 상태로
> 며칠이 지나갔고, 워크플로는 계속 성공이라고 말했습니다(`continue-on-error`).
> 규칙에 대한 설명은 이 문서에 씁니다.

아이폰은 **홈 화면에 추가한 앱에서만** 푸시를 받는다. 데스크톱 셸(WKWebView)에는
Push API가 아예 없어서, 열려 있는 동안은 앱 자체가 알리고 닫혀 있으면 폰이 받는다.
그래서 `pushSupport()`는 참/거짓이 아니라 **이유**를 돌려준다 — 아무 말 없이 아무
일도 안 하는 스위치가 스위치 없는 것보다 나쁘다.

## 오늘 노트 (`/dailyNotes/{이메일키}/{날짜}`)

하루에 하나, 사람마다 하나. `{ html, at }` 뿐입니다.

**개인 가지에 삽니다.** 프로젝트 밑이 아니고, 공유되지 않고, 규칙도 본인만
읽고 씁니다 — `notices`와 달리 남이 쓸 수 있는 구멍조차 없습니다. 여기 적히는
건 '커피 주문' 같은, 오늘이 지나면 아무 의미 없는 것들입니다. 50명이 함께 보는
곳에 두면 아무도 거기에 그런 걸 안 씁니다.

노트 안에는 두 종류의 줄이 있습니다.

- **자유 체크리스트** — 손으로 친 글자. 이 노트 밖으로 나가지 않습니다.
- **업무 참조** — `<div data-task-ref data-task-id="...">`. **id만 저장하고
  이름·상태·마감일은 그릴 때마다 태스크에서 읽습니다.** 그래서 남이 이름을
  바꾸거나 끝내면 내 노트에도 반영됩니다. 이름을 복사해 두었다면 오늘 아침의
  사본을 붙들고 하루를 보내게 됩니다.

체크는 한 방향으로만 흐릅니다: 노트에서 체크하면 그 태스크가 완료가 됩니다.
담당자·마감일·프로젝트는 여기서 못 고칩니다 — 이름을 누르면 상세 창이 열리고,
거기가 그걸 고치는 곳입니다. **노트는 일이 사는 곳이 아니라 오늘의 결정이 사는
곳입니다.**

### 노트 안의 자료

`/`를 치고 '자료'를 고르면 드라이브에서 찾아 한 줄로 붙습니다. 업무 참조와 같은
규칙입니다 — **아이디만 저장하고 이름은 그릴 때마다 읽습니다.** 드라이브에서
이름을 바꾸면 노트에도 바뀝니다. 붙일 때의 이름은 사본으로 하나 갖고 있는데,
드라이브가 연결 안 돼 있거나 권한이 없을 때 빈 줄이 되지 않기 위해서입니다.

검색 상자는 업무 상세 창에서 파일을 붙일 때 쓰는 것과 같은 것(`DriveSearch`)이고,
폴더로 좁히지 않습니다 — 노트는 프로젝트에 속하지 않으니까요.

## 첨부 파일 변경 (`/driveWatch/{이메일키}`)

받은 알림에 **밖에서 온 소식**을 들이는 첫 자리입니다. 슬랙·메일까지 다
흘려보내는 대신 하나만 골랐습니다 — 슬랙 알림은 슬랙이 더 잘 보여 주고, 메일은
메일함이 더 잘 보여 줍니다. 여기서만 할 수 있는 말은 하나입니다: **지금 바뀐 그
파일이, 당신이 들고 있는 어느 업무에 붙어 있는가.** 드라이브는 파일이 바뀐 것까지
알고, 그게 '3화 대본 검수'에 붙어 있다는 건 이 앱만 압니다.

```
driveWatch/{이메일키}/
  token          지난번까지 봤다는 표식 (Drive changes page token)
  seen/{파일ID}  그 파일로 마지막에 알린 시각(ms)
```

- **본인만 읽고 씁니다.** 규칙은 `dailyNotes`와 같은 모양입니다.
- 보는 대상은 **내가 담당인, 안 끝난 업무에 붙은** 드라이브 파일뿐입니다.
  프로젝트 전체가 아닙니다.
- 파일마다 묻지 않고 **드라이브 변경 목록**(`changes.list`)을 씁니다. 파일이
  몇 개든 요청은 한 번입니다.
- **처음 켠 날에는 표식만 찍고 아무것도 안 알립니다.** 안 그러면 첫 실행에
  그동안의 모든 수정이 한꺼번에 쏟아집니다.
- 같은 파일로 다시 알리기까지 6시간을 둡니다. 온종일 고치는 문서 하나가 목록을
  다 먹지 않게.
- 알림 키가 `dr_{파일ID}_{수정시각}`이라 기기 두 대가 같은 변경을 봐도 줄은
  하나입니다.
- 줄을 누르면 파일이 아니라 **업무**가 열립니다. 파일만 열면 드라이브 알림과
  똑같아지고, 이 줄이 여기 있을 이유가 없어집니다.

## 메일 (저장하지 않음)

받은 알림의 두 번째 외부 소식입니다. 저장 위치가 **없습니다** — 이 절이
`data-model.md`에 있는 이유가 그것입니다.

브라우저가 구글에게 직접 토큰을 받아 직접 물어보고, 화면에만 씁니다. 데이터
베이스에 복사해 두면 세 가지가 생깁니다: 지메일에서 읽었는데 여기서는 안 읽음
으로 남는 어긋남, 우리 쪽에도 '읽음'을 만들어야 하는 일, 그리고 회사 메일 제목이
남의 서비스에 한 벌 더 놓이는 것. 볼 때 물어보고, 안 볼 때는 아무 데도 없습니다.

가져오는 기준은 '안 읽은 메일'이 아니라 **나에게 물어 왔고 아직 내가 답 안 한
대화**입니다:

```
in:inbox is:unread to:me -from:me
-category:promotions -category:social -category:updates -category:forums
```

여기에 질의로는 못 하는 조건 하나를 코드에서 더 겁니다 — **스레드의 마지막 글이
나면 뺍니다.** 내가 마지막으로 말한 대화는 내 차례가 아닙니다.

- 권한은 `gmail.readonly` 하나. 보내지도 지우지도 않습니다.
- 4분에 한 번, 앱이 떠 있는 동안. 목록을 열어야 맞는 숫자가 되면 배지가 아닙니다.
- **운영체제 배지에는 안 셉니다.** 같은 폰의 지메일 앱이 이미 그 메일을 세고
  있고, 한 통을 두 아이콘이 세면 둘 다 못 믿게 됩니다. 앱 안의 탭 배지에는
  셉니다 — 그 목록에 실제로 들어 있으니까요.

### 구글 클라우드 쪽에서 해야 하는 것

1. **Gmail API 사용 설정** — APIs & Services → Library → Gmail API.
2. **OAuth 동의 화면에 `gmail.readonly` 추가.** 제한된 범위라 외부 앱이면 검수가
   필요하지만, 이 동의 화면은 조직 내부(Internal)라 그대로 됩니다.

### 슬랙은 왜 아직 없나

브라우저만으로는 안 됩니다. 슬랙 OAuth는 코드를 토큰으로 바꿀 때 `client_secret`을
요구하는데(PKCE 공개 클라이언트 흐름이 없습니다) 그 값을 브라우저에 둘 수 없고,
슬랙 Web API는 사용자 토큰을 브라우저에서 쓰는 것을 허용하지 않습니다. 즉 서버가
필요합니다 — `mcp/`의 Cloud Run 서비스가 그 자리입니다.

필요한 것:

- Cloud Run 배포가 되어야 합니다. 지금은 GitHub 저장소 비밀값 네 개
  (`GCP_SA_KEY`, `MCP_PUBLIC_URL`, `GOOGLE_OAUTH_CLIENT_ID`,
  `GOOGLE_OAUTH_CLIENT_SECRET`)가 없어서 워크플로가 첫 검사에서 멈춥니다.
- 슬랙 워크스페이스에 앱을 만들고 사용자 범위 `im:history`, `im:read`,
  `users:read`, `search:read`를 받아야 합니다. 사람마다 한 번씩 설치합니다.
- 가져올 것은 **DM과 멘션만.** 채널 글은 안 가져옵니다 — 슬랙 알림을 통째로
  옮기면 이 목록은 일주일 안에 죽습니다.

## 조직과 회의실 (`/orgs/{조직id}`, `/orgByDomain/{도메인키}`)

회의를 만들 때 회의실까지 한 번에 잡기 위한 것입니다. 지금까지는 앱에서 일정을
만들고, 따로 예약 사이트에 들어가 같은 시간을 한 번 더 입력해야 했습니다. 같은
결정을 두 번 적는 일이고, 두 번째를 잊으면 회의실 없는 회의가 됩니다.

```
orgs/{조직id}/
  meta: { name, domain, createdBy, createdAt }
  rooms/{방id}: { name, note?, order, active }
  bookings/{날짜}/{예약id}: { roomId, from, to, by, byName?, title?, eventId?, at }
orgByDomain/{도메인키}: 조직id
```

### 이것이 프로젝트 멤버십을 대신하지 않습니다

`CLAUDE.md`가 말하는 규칙 — *접근 범위는 프로젝트 멤버십으로만* — 은 그대로입니다.
업무와 프로젝트가 누구에게 보이는지는 계속 프로젝트 멤버십만으로 정해집니다.
조직은 **'우리 회사에 회의실이 셋 있다'는 공유된 사실**을 담는 자리이고, 누가
무슨 업무를 볼 수 있는지와는 아무 상관이 없습니다. 여기에 접근 개념을 하나 더
만들면 축이 두 개가 되고, 축이 두 개면 언젠가 어긋납니다.

### 소속은 이메일 도메인

초대도 승인도 없습니다. `@bpp.co.kr`로 로그인했으면 우리 회사고, 그건 이미 참인
사실이라 따로 관리할 게 없습니다. 규칙이 그대로 검사합니다:

```
auth.token.email.toLowerCase().replace('.', ',')
  .endsWith('@' + root.child('orgs/{조직id}/meta/domain').val().replace('.', ','))
```

`email_verified`를 같이 봅니다 — 확인 안 된 주소로 도메인을 주장할 수 있으면
검사가 아닙니다. 구글 로그인만 쓰므로 실제로는 늘 참입니다.

`orgByDomain`은 **한 번만 쓸 수 있습니다**(`!data.exists()`). 한 도메인에 조직
하나고, 먼저 만든 것이 그 조직입니다. 자기 도메인의 항목만 읽습니다.

만들 때는 `meta`를 **먼저** 씁니다. 규칙이 도메인을 `meta`에서 읽으므로, 색인이
먼저 생기면 아무도 그 조직에 아무것도 못 씁니다.

### 조직에 공개된 프로젝트 (`orgs/{조직id}/projects`)

새로 들어온 사람이 아무것도 못 보는 문제를 풉니다. 지금까지는 프로젝트마다
누군가 초대해 주기 전에는 회사에 무슨 프로젝트가 있는지조차 알 수 없었습니다.

**경계가 아니라 라벨입니다.** 목록에 이름이 올라와도 그 프로젝트의 업무는 못
봅니다 — 접근은 계속 `projects/{pid}/members`가 정합니다. 이 목록이 답하는 건
하나입니다: *우리 회사에 이런 프로젝트가 있고, 들어가려면 누구에게 말하면 되는가.*
`CLAUDE.md`의 "라벨은 라벨이고, 경계가 아닙니다"가 그대로 적용되는 자리입니다.

```
orgs/{조직id}/projects/{pid}: { name, color?, by, at }
orgs/{조직id}/joinRequests/{pid}/{이메일키}: { at, name? }
```

- 이름을 **한 벌 베껴 둡니다.** 프로젝트 노드는 멤버 아닌 사람에게 닫혀 있어서,
  목록을 그리려면 그 안을 읽지 않고도 이름을 알아야 합니다. 초대장
  (`invitesByEmail`)이 이미 같은 이유로 같은 일을 하고 있습니다. 사본은 늙으므로,
  이름을 바꿀 때 같이 고칩니다(`syncProjectName`).
- 올리고 내리는 것은 **그 프로젝트 멤버**가 합니다(사이드바 우클릭 → 조직에 공개).
  조직 관리자가 아닙니다 — 관리자에게 프로젝트 권한을 주지 않기 위해서고, 남의
  프로젝트를 끌어오는 일도 없어야 합니다. 규칙이 `projects/$pid/members/$uid`를
  직접 검사합니다.
- 내릴 때 그 프로젝트의 요청도 같이 치웁니다. 목록에 없는 프로젝트에 대한
  요청은 아무도 볼 데가 없는 채로 남습니다.
- **참여 요청 목록은 조직원 전체가 읽습니다.** 리스너 하나로 덮기 위해서고,
  '누가 어디 참여를 요청했다'는 프로젝트 **내용**이 아닙니다 — 그 프로젝트가
  조직 안에 있다는 사실 자체가 이미 조직에 공개된 것이니까요. 다만 **승인은
  그 프로젝트 멤버만** 합니다.
- **승인은 초대장을 쓰는 것입니다.** 이미 있는 초대 흐름(`addMember` →
  `invitesByEmail` → 상대 앱의 초대 창)을 그대로 씁니다. 참여 경로를 두 개
  만들면 하나는 언젠가 안 맞게 됩니다.

### 관리자 (`orgs/{조직id}/admins/{이메일키}`)

회의실 목록은 쉰 명이 함께 보는 값입니다. 누구나 방을 지울 수 있으면 어제 잡아 둔
회의실이 오늘 없어져 있을 수 있고, 그걸 되돌릴 사람도 정해져 있지 않습니다.

- **조직을 만든 사람이 첫 관리자**입니다. 색인(`orgByDomain`)보다 **먼저**
  씁니다 — 색인이 먼저 생기면 다른 사람 화면에 조직이 뜨는데 관리자는 아직
  없고, 그 틈에 아무나 자기를 관리자로 만들 수 있습니다.
- 관리자는 **우리 도메인 주소만** 될 수 있습니다. 회의실 목록을 바깥 사람이
  고칠 수 있으면 조직이라는 말이 무의미해집니다.
- **관리자가 아무도 없는 조직은 아무나 가져갈 수 있습니다.** 규칙의 안전장치고,
  관리자가 전부 사라진 조직이 영원히 손 못 대는 것으로 남지 않게 합니다.
  그래서 화면에서는 **마지막 관리자가 스스로 못 나갑니다** — 나가려면 다음
  사람을 먼저 세웁니다.
- 관리자가 아닌 사람에게도 **누가 관리자인지** 보여줍니다. 잠긴 버튼만 있고
  누구에게 말해야 하는지 없으면 그건 막다른 길입니다.

**이 힘은 회의실 목록에만 미칩니다.** 업무와 프로젝트가 누구에게 보이는지는
계속 프로젝트 멤버십만으로 정해집니다 — 조직 관리자가 남의 업무를 볼 수 있게
되면 접근 축이 두 개가 되고, 축이 두 개면 언젠가 어긋납니다. `CLAUDE.md`가
말하는 그 규칙입니다.

**잠근 것은 목록이지 사용이 아닙니다.** 예약은 조직 전원이 합니다. 회의실을
쓰려고 관리자에게 부탁해야 한다면 이 기능은 없는 게 낫습니다.

### 방 이름은 구글 일정의 '장소'에도 적습니다

예약은 우리 데이터베이스에 있고, 그건 조직원만 읽습니다. 그런데 프로젝트에는
**도메인 밖 사람도 있고**(출연자, 외주), 애초에 이 앱을 안 쓰는 사람도 있습니다.
그들에게 "이 회의 어디서 하지"를 답해 줄 유일한 공통 자리가 구글 일정의 장소
칸입니다. 예약은 우리가 관리하고, 결과는 모두가 보는 곳에 적습니다.

새 일정은 **만들 때 같이** 넣습니다 — 나중에 붙이면 이미 나간 초대 메일에는
장소가 없고, 메일만 보는 사람에게는 그게 전부입니다.

예약을 풀 때는 **그 방 이름일 때만** 장소를 지웁니다. 사람이 손으로 적어 둔
장소('3층 로비', 줌 링크)를 예약 취소를 이유로 지우면, 우리가 쓰지도 않은 값을
우리가 없앤 것입니다.

### 도메인 밖 프로젝트 멤버

프로젝트에 `@bpp.co.kr`이 아닌 사람이 있는 것은 정상입니다. 그들은 조직원이
아니므로:

- 회의실 목록도, 예약도 못 읽고 못 씁니다. 규칙이 도메인으로 막습니다.
- 일정 카드에 회의실 칸이 **아예 안 보입니다.** '권한이 없습니다'를 띄우지
  않습니다 — 자기가 할 수 없는 일에 대한 설명은 대체로 소음입니다.
- 다만 회의 장소는 봅니다. 위의 '장소' 칸이 그 이유입니다.

### 예약은 날짜가 먼저

`bookings/{날짜}/{예약id}` 순서입니다. 화면이 묻는 건 늘 "이 날 이 방 비었나"인데,
방이 먼저면 방마다 리스너가 하나씩 필요합니다. 날짜가 먼저면 하루에 하나로 모든
방을 덮습니다. 타임라인이 보는 날짜만 구독하고, 벗어난 날짜는 놓습니다.

시간 단위는 **자정부터 분**입니다. 타임라인이 쓰는 단위와 같아서 변환이 없습니다.

겹침 판정에서 **끝과 시작이 같은 것은 겹침이 아닙니다.** 2시에 끝나는 회의와 2시에
시작하는 회의는 같은 방을 쓸 수 있고, 그게 회의실이 돌아가는 방식입니다.

남의 예약은 못 건드립니다(`data.child('by').val() === auth.token.email`). 회의실을
뺏는 일이 조용히 일어나면 안 됩니다.

### 일정과 예약이 어긋나지 않게

예약은 `eventId`로 자기가 어느 회의의 것인지 기억합니다. 그래서:

- **새 일정**은 구글이 일정을 만들어 준 **다음에** 방을 잡습니다. 그 전에는 id가
  없고, 주인 없는 예약은 아무도 치울 수 없습니다.
- 일정 만들기가 실패하면 방도 안 잡습니다.
- **일정을 지우면** 방을 먼저 풉니다. 일정이 사라진 뒤에는 어느 예약이 그 일정
  것이었는지 알 방법이 없습니다.
- **일정을 옮기면** 예약도 따라갑니다. 안 따라가면 3시로 미룬 회의의 방이 2시에
  잡혀 있고, 3시에는 남이 그 방을 잡을 수 있습니다 — 화면에는 아무 문제가 없어
  보이고, 회의 시간에 방에 가면 다른 팀이 있습니다.
- 옮긴 시간에 이미 남의 예약이 있으면 **옮기지 않고 말해 줍니다.** 조용히 풀면
  방 없는 회의가 되고, 억지로 겹치면 두 팀이 같은 방에 갑니다.

### 방은 지우지 않고 끕니다

`active: false`. 방을 지우면 그 방으로 잡아 둔 지난 예약들이 이름을 잃습니다.
'(삭제된 회의실)'이 되는 것보다, 새로 못 잡는 방으로 남는 편이 낫습니다.

### 앞으로 — 노션식 층 구조

지금은 층이 **관리자 / 조직원** 둘이고, 그 힘은 회의실에만 미칩니다. 조직이
실제로 쓰이기 시작하면 노션 워크스페이스처럼 **소유자 / 관리자 / 멤버 / 게스트**
로 나누는 것이 다음 단계입니다. 그때 지킬 것을 미리 적어 둡니다:

- **게스트가 먼저 필요합니다.** 도메인 밖 프로젝트 멤버가 이미 있고, 지금은
  '조직원이 아님'으로만 표현됩니다. 그들에게 줄 것과 안 줄 것을 이름 있는
  자리로 만들어야 합니다.
- **소유자는 관리자를 정하는 사람**이고, 그 하나 때문에 층이 필요합니다.
  지금은 관리자끼리 서로를 해제할 수 있습니다.
- 그래도 **업무·프로젝트 접근은 프로젝트 멤버십**입니다. 층이 늘어나는 것은
  조직 설정을 누가 고치는가의 문제고, 누가 무슨 업무를 보는가로 넘어오면
  접근 축이 두 개가 됩니다.

### 아직 안 한 것

- 기존 예약 사이트(`bppreserv`)의 예약을 가져오지 않습니다. 두 곳이 각자
  기록하는 동안은 **양쪽을 다 봐야** 합니다. 그 사이트를 접을 때 한 번 옮기면
  되는 일이고, 그때까지는 이 앱에서 잡은 것만 이 앱이 압니다.
- 장비 예약, 매주 반복은 없습니다.
- 회의실을 구글 캘린더 리소스로 두는 길도 있습니다(구글이 이중 예약을 직접
  막아 주고, 앱을 안 쓰는 사람도 구글 캘린더에서 방 일정을 봅니다). 방 데이터에
  캘린더 id를 붙일 자리를 비워 두었으니 나중에 얹을 수 있습니다.
