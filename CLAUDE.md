# bpp-ops

블랙페이퍼(BPP)의 업무 관리 도구. 웹 + macOS 데스크톱 앱 + Claude MCP 커넥터.

## 누가 쓰는가

**블랙페이퍼 전원, 50명 남짓.** 소규모 팀이 아닙니다. 설계할 때 이 규모를 전제로
판단해야 합니다:

- **한 사람의 조작이 전원에게 보이는 것**과 **개인 설정**을 구분해야 합니다.
  프로젝트의 성격(예: 그룹 이름)은 공유하되, 배열 순서 같은 취향은 개인 것으로
  두는 편이 낫습니다. 50명이 같은 목록의 순서를 놓고 서로 덮어쓰면 아무도
  자기 화면을 신뢰할 수 없습니다.
- **접근 범위는 프로젝트 멤버십으로만** 정해집니다. 새 기능이 새로운 권한 개념을
  만들지 않도록 합니다 — 라벨은 라벨이고, 경계가 아닙니다.
- 화면에 프로젝트가 수십 개 놓일 수 있다는 뜻이므로, 목록은 접히고 묶여야 합니다.

## 사용자에 대해

- 이 저장소의 주 사용자는 **개발자가 아닙니다.** 한국어로, 간결하게 답합니다.
- 작업 흐름: 구현 → 빌드/테스트 → `claude/*` 브랜치 푸시 → main 병합(웹 자동 배포)
  → 배포 번호 확인 후 보고.
- 릴리즈(.dmg)는 `CHANGELOG.md` 최상단에 `## vX.Y.Z` 절을 추가하고 main에 올리면
  나갑니다. 버전은 `package.json` · `src-tauri/tauri.conf.json` · `src-tauri/Cargo.toml`
  세 곳이 같아야 하고, 다르면 워크플로가 멈춥니다.

## 구조에서 자주 놓치는 것

- **데스크톱 앱에는 화면 코드가 없습니다.** 배포된 웹(`crng-task-manager.web.app`)을
  실행 시점에 불러옵니다. 웹 배포 = 앱도 최신. `.dmg` 재설치는 셸(Rust) 자체가
  바뀔 때만 필요합니다.
- **웹뷰에서 조용히 실패하는 브라우저 기능들이 있습니다** — `confirm()`,
  `target="_blank"`, 구글 로그인 팝업 등. 목록과 대체 수단은
  `docs/desktop-updates.md` 하단 표에 있습니다. 새 기능 만들 때 확인할 것.
- 데이터는 프로젝트 단위로 나뉘어 있고, DB 규칙이 프로젝트 멤버십을 직접
  검사합니다. `docs/data-model.md` 참고.
